---
- name: Prepare system
  hosts: all
  remote_user: root
  roles:
  - ansible-role-golang
  - cli-tools

- name: Configure pseudo-root user system
  hosts: all
  remote_user: root
  roles:
  - sudo-user
...
