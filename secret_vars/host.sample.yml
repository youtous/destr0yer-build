# vim: ts=2 sw=2 et
---
# Variables listed here are not committed and are specific to a named host
# rename the file using {$host}.yml format

# generate password using  (change password, SALT)
# openssl passwd -6
arbiter_password: ""
arbiter_clear_password: ""

root_password: ""