---
# Variables listed here are not committed and are shared between all hosts
# Moreover this file should be protected using
# `env EDITOR=vim ansible-vault edit secret_vars/all.yml`

# name of the arbiter
arbiter_username: "john"

arbiter_ssh_keys:
  - "list keys here"
