# hosts that must receive a first configuration for server
factoring_destroyers:
  hosts:

# destr0yers under a commander, they are accessible from ssh only from ssh_entrypoints
base_destr0yers:
  hosts:
    heaven-pascal.youtous.dv:
    heaven-roberval.youtous.dv:

# destr0yers that are public exposed
commander_destr0yers:

# hosts that will be able to deliver mails using an smtp relay
smtp_relays:
  hosts:
    heaven-pascal.youtous.dv:
    heaven-roberval.youtous.dv:

# hosts with a database server
database_servers:
  hosts:
    heaven-pascal.youtous.dv:
# client who connect to database servers
database_clients:
  hosts:
    heaven-pascal.youtous.dv:
    heaven-roberval.youtous.dv:

# hosts that will be backed-up on a backup-storage
backed_up_hosts:
  hosts:
    heaven-pascal.youtous.dv:
    heaven-roberval.youtous.dv:

# hosts where ip is monitored
watch_ip:
  #hosts:
  #  heaven-pascal.youtous.dv:

# hosts which are monitoring ssl hosts, /!\ docker MUST be installed
watch_ssl:
  hosts:
    heaven-pascal.youtous.dv:

# hosts with specific interfaces configuration
interfaces:

# hosts used as backup server
backup_storage:
  hosts:
    heaven-pascal.youtous.dv:

# hosts used as an WireGuard server
wireguard_servers:
  hosts:
    heaven-pascal.youtous.dv:

# hosts using a VPN WireGuard server
wireguard_clients:
#  hosts:
#    heaven-roberval.youtous.dv: # disabled for tests only