---
- name: restart rsyslog
  become: true
  systemd:
    name: rsyslog
    state: restarted

- name: daemon-reload systemd
  become: true
  systemd:
    daemon_reload: true

- name: restart docker
  throttle: 1 # docker should not be restarted globally
  become: true
  systemd:
    name: docker
    state: restarted
...
