#!/usr/bin/env bash

echo "Calling postgresql backup script..."
su - postgres -c 'pg_dumpall' > {{ postgresql_backup_output }}/backup_cluster.sql
echo "Postgresql databases backed-up to {{ postgresql_backup_output }}..."