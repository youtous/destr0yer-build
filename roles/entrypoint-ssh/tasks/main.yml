---
# This playbook contains settings that will only allow SSH connections from
# a specific authoritative server
- name: Generate list of allowed ssh entrypoints
  become: true
  template:
      src: files/hosts.allow.j2
      dest: /etc/hosts.allow
      owner: root
      group: root
      mode: u=rw,g=r,o=r