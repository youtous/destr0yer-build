---
timezone: "Etc/UTC"

system_configuration_server_environment: "{{ server_environment | default('dev') }}"

system_folders_write_root_only:
  - /etc
  - /dev
  - /bin
  - /boot
  - /lib
  - /media
  - /mnt
  - /usr
  - /opt
  - /proc
  - /run
  - /sbin
  - /srv
  - /var
...
