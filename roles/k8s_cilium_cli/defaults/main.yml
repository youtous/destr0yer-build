# renovate: datasource=github-releases depName=cilium/cilium-cli
k8s_cilium_cli_version: v0.19.4

k8s_cilium_cli_os: "linux"
k8s_cilium_cli_arch: "{{ 'arm64' if ansible_architecture == 'aarch64' else 'amd64' }}"
