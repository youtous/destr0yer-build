---
apiVersion: v1
kind: Secret
metadata:
  namespace: "{{ k3s_promgraf_k8s_namespace }}"
  name: "{{ k3s_promgraf_basic_auth_secret_name }}"
type: Opaque
data:
{% for authuser in k3s_promgraf_basic_auth_users %}
  {{ authuser.username }}: "{{ authuser.password | b64encode }}"
{% endfor %}
...
