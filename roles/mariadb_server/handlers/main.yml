---
- name: Restart mariadb
  become: true
  ansible.builtin.systemd:
    state: restarted
    name: mariadb

- name: Systemd daemon reload
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

...
