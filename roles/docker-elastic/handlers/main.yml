---
- name: Import docker configs secrets handler
  include_tasks: docker-config-secrets.yml

- name: docker prune
  community.general.docker_prune:
    containers: true
    images: true
    networks: true
    builder_cache: true
...
