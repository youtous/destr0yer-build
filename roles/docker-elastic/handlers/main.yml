---
- name: deploy elastic
  docker_stack:
    state: present
    name: elastic
    compose:
      - "{{ elastic_compose_file_dest }}"
  notify: docker prune

- name: docker prune
  docker_prune:
    containers: yes
    images: yes
    networks: yes
    builder_cache: yes
...