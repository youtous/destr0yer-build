filter {
  if ("external_input" in [tags]) {
      # import beat metadata
      if [_cluster_meta][beat] {
          mutate {
            rename => { "[_cluster_meta][beat]" => "[@metadata][beat]" }
          }
      }
  }
}