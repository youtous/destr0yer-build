filter {
    if [@metadata][beat] == "journalbeat" {
      if [message] =~ "^ *{" {
            json {
              source => "message"
            }
            if "_jsonparsefailure" in [tags] {
              drop {}
            }
            mutate {
              remove_field => ["message"]
            }
        }
    }
}