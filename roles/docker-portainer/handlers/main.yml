---
- name: deploy portainer
  docker_stack:
    state: present
    prune: yes
    name: portainer
    compose:
      - "{{ portainer_compose_file_dest }}"
  notify: docker prune

- name: docker prune
  docker_prune:
    containers: yes
    images: yes
    networks: yes
    builder_cache: yes
...