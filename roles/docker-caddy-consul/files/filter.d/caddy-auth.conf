[INCLUDES]

before = common-docker.conf

# TODO : fix basic auth, not working atm

[Init]
journalmatch = CONTAINER_TAG=caddy-consul

[Definition]

prefregex = ^(?:%(__docker_systemd_prefix_re)s\s)<F-CONTENT>.+</F-CONTENT>$

failregex = ^<HOST>.+\s(400|401)\s.+

# delegate date to journalctl
datepattern=