apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: "{{ namespace }}"
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
