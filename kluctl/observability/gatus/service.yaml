apiVersion: v1
kind: Service
metadata:
  name: gatus
  namespace: observability
  labels:
    app.kubernetes.io/name: gatus
    app.kubernetes.io/component: uptime-monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gatus
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
